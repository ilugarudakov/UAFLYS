# DEV 2025

```markdown
# Тестовое задание: Поиск доступных авиамаршрутов
## 🎯 Цель задания
Разработать систему для поиска маршрутов между аэропортами с возможностью фильтрации 
по авиакомпаниям, маршрутам с пересадками и отображением информации о рейсах. 
Система состоит из backend-части на PHP и frontend-интерфейса.
### 1. Общая постановка задачи
Необходимо:
- Создать базу данных маршрутов между аэропортами.
- Импортировать данные о рейсах из открытых источников (указано в примечании) 
по 5 аэропортам.
- Реализовать поиск маршрутов:
    - прямых
    - с одной пересадкой
    - с двумя пересадками
- Фильтрация по авиакомпании.
- Интерфейс, позволяющий выбрать аэропорт отправления и назначения, фильтры 
и просматривать найденные маршруты.
### 2. Backend (PHP)
## Требования:
- Использовать чистый PHP без тяжелых фреймворков (Laravel, Symfony и т.п.).
- Разрешено использовать composer-библиотеки (например, для подключения к БД 
или работы с HTTP), при этом:
- Необходимо обосновать выбор сторонних библиотек в README.md.
- Поддержка REST API (можно использовать простой роутинг).
- Предусмотреть структуру проекта для масштабируемости.
## Функциональность:
- Инициализация базы данных (SQL-скрипт или PHP-скрипт).
- Импорт открытых данных в БД.
- API-эндпоинты:
    - /airports – список аэропортов.
    - /airlines – список авиакомпаний.
    - /routes – список маршрутов по заданным параметрам:
        --аэропорт отправления
        --аэропорт назначения
        --фильтр по авиакомпании
        --глубина поиска: прямой, 1 пересадка, 2 пересадки
### 3.Frontend 
## Требования:
- Использовать любой готовый фреймворк
- Упор на UX: интуитивно понятный выбор аэропортов и фильтров.
Интерфейс должен включать:
- Выбор аэропорта отправления и назначения.
- Фильтр по авиакомпании.
- Переключатель: прямой рейс / рейсы с пересадками.
- Таблица с результатами (рейс, авиакомпания, путь, время, количество пересадок и т.п.)
### 4.Источники данных
- Разработчик сам выбирает открытые источники данных:
- Необходимо задокументировать используемые источники и показать, как выполняется 
импорт данных.
### 5.Требования к сдаче
- Репозиторий на GitHub или ZIP-архив.
- Четкая структура проекта.
- README.md с:
    - кратким описанием архитектуры,
    - инструкцией по запуску frontend и backend,
    - описанием используемых библиотек и обоснованием их выбора,
    - описанием формата импорта данных.
- Возможность развернуть проект локально.
## Примечания
- Можно использовать SQLite для ускорения разработки.
- данные из открытых источников для 5 аэропортов: FCO, AYT, OTP, DUS, IST

```
