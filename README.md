# Поиск авиамаршрутов (Demo)

## Запуск проекта

Из корня проекта выполните:

```bash

docker compose up --build -d

```

API доступно по адресу http://localhost:8000

Статическая фронтенд-страница по адресу http://localhost:3000